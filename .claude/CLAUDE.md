# Global Claude Code instructions

Rules and preferences that should apply across every working directory.
Per-project context belongs in that project's own `CLAUDE.md` or in
`~/.claude/projects/<dir>/memory/`, not here.
